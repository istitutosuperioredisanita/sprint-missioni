'use strict';

angular.module('missioniApp')
    .config(function ($stateProvider) {
        $stateProvider
            .state('***REMOVED***', {
                abstract: true,
                parent: 'site'
            });
    });
